name: 🚀 Deploy website on push

on: 
  push:
    branches:
      - master  # Correctly indented

jobs:
  web-deploy:
    name: 🎉 Deploy website
    runs-on: ubuntu-latest
    steps:
      # Checkout the repository
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      # Sync files via FTP
      - name: 📂 Sync files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: "103.131.95.91"
          username: "superadmin@mtwd-kit.ph"
          password: "vG[deoNwoNVU"
          protocol: ftps
